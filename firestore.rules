rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The authenticated server applies owner checks. Browsers have no direct access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
